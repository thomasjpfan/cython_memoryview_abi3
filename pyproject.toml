[build-system]
requires = ["setuptools", "setuptools-scm", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "adder"
version = "0.0.1"
dynamic = ["requires-python"]

[tool.setuptools_scm]
